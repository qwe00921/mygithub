<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <View
        android:id="@+id/divider"
        style="@style/global_divider_2"
        android:layout_height="0.5dp" />

    <RelativeLayout
        android:id="@+id/rl_root_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="8dp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingTop="8dp" >

        <EditText
            android:id="@+id/global_comment_inputbox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:layout_marginRight="10dp"
            android:layout_toLeftOf="@+id/global_comment_submit"
            android:layout_toStartOf="@+id/global_comment_submit"
            android:background="@drawable/global_inputbox_bg_dark"
            android:drawableLeft="@drawable/edit_icon"
            android:drawableStart="@drawable/edit_icon"
            android:gravity="center_vertical"
            android:maxLines="4"
            android:padding="4dp"
            android:singleLine="false"
            android:maxLength="140"
            android:textColor="@color/white" />

        <Button
            android:id="@id/global_comment_submit"
            android:layout_width="62dp"
            android:layout_height="28dp"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:background="@drawable/my_login_btn_selector"
            android:text="@string/article_comment_list_title"
            android:textColor="@color/global_white"
            android:textSize="18sp" />
    </RelativeLayout>

</LinearLayout>